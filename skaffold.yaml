apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/bygui86/awsomeapp:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/bygui86/awsomeapp:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: awsomeapp
        chartPath: charts/awsomeapp
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/bygui86/awsomeapp"
          image.tag: "{{.DIGEST_HEX}}"
